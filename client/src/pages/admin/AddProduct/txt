</Card>

      {/* Pricing */}
      <Card>
        <CardHeader>
          <h2 className="text-lg font-medium">Pricing</h2>
        </CardHeader>
        <CardContent>
          {/* <Form {...form}>
            <FormField control={form.control} name="price" render={({ field }) => (
              <FormItem>
                <FormLabel>Price</FormLabel>
                <FormControl>
                  <Input type="number" placeholder="0.00" {...field} />
                </FormControl>
                <FormMessage />
              </FormItem>
            )} />
          </Form> */}
        </CardContent>
      </Card>

      {/* Variants */}
      <Card>
        <CardHeader>
          <h2 className="text-lg font-medium">Variants</h2>
        </CardHeader>
        <CardContent>
          {/* <Form {...form}>
            <FormField control={form.control} name="size" render={({ field }) => (
              <FormItem>
                <FormLabel>Size</FormLabel>
                <FormControl>
                  <Input placeholder="Enter size" {...field} />
                </FormControl>
                <FormMessage />
              </FormItem>
            )} />
          </Form> */}
        </CardContent>
      </Card>

      {/* Inventory */}
      <Card>
        <CardHeader>
          <h2 className="text-lg font-medium">Inventory</h2>
        </CardHeader>
        <CardContent>
          {/* <Form {...form}>
            <FormField control={form.control} name="trackQuantity" render={({ field }) => (
              <FormItem>
                <FormControl>
                  <Checkbox {...field} /> Track quantity
                </FormControl>
                <FormMessage />
              </FormItem>
            )} />
          </Form> */}
        </CardContent>
      </Card>

      {/* Shipping */}
      <Card>
        <CardHeader>
          <h2 className="text-lg font-medium">Shipping</h2>
        </CardHeader>
        <CardContent>
          {/* <Form {...form}>
            <FormField control={form.control} name="physicalProduct" render={({ field }) => (
              <FormItem>
                <FormControl>
                  <Checkbox {...field} /> This is a physical product
                </FormControl>
                <FormMessage />
              </FormItem>
            )} />
          </Form> */}
        </CardContent>
      </Card>


      <CardFooter>
        {/* <Button type="submit" onClick={form.handleSubmit(onSubmit)}>Save Product</Button> */}
      </CardFooter>